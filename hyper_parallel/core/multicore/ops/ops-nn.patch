diff --git a/activation/swi_glu/op_kernel/glu_tiling_kernel.hpp b/activation/swi_glu/op_kernel/glu_tiling_kernel.hpp
index f0d3a2c9..20e21ca7 100644
--- a/activation/swi_glu/op_kernel/glu_tiling_kernel.hpp
+++ b/activation/swi_glu/op_kernel/glu_tiling_kernel.hpp
@@ -76,7 +76,7 @@ struct SwigluSingleTilingKernel {
 
   // calc tiling data
   __aicore__ void GetTilingAndOffset(GM_ADDR tiling_gm, uint32_t inputDTypeLen) {
-    GET_TILING_DATA(tempTilingGm, tiling_gm);
+    GET_TILING_DATA_WITH_STRUCT(SwiGluTilingData, tempTilingGm, tiling_gm);
 
     is32BAligned = tempTilingGm.is32BAligned;
     rowLen = tempTilingGm.rowLen;
@@ -202,8 +202,8 @@ do {                                \
 } while (0)
 #define SWIGLU_SINGLE_PROCESS(kernelTiling) \
 do {                                       \
-    uint64_t blockNum = GetBlockNum();       \
-	  for(uint64_t tileIdx = AscendC::GetBlockIdx(); tileIdx < (kernelTiling).totalTileNum; tileIdx += blockNum) { \
+    uint64_t blockNum = 1;       \
+	  for(uint64_t tileIdx = 0; tileIdx < (kernelTiling).totalTileNum; tileIdx += blockNum) { \
         (kernelTiling).CaclOneTileParam(tileIdx); \
         SWIGLU_SINGLE_PROCESS_TILE((kernelTiling).offsetParam, *((kernelTiling).curTileCopyParam), (kernelTiling).curCalLen); \
 	} \
@@ -216,8 +216,8 @@ do {                                              \
 } while(0)
 #define SWIGLU_SINGLE_PROCESS_NON32BALIGNED(kernelTiling) \
 do {                                       \
-    uint64_t blockNum = GetBlockNum();       \
-	  for(uint64_t tileIdx = AscendC::GetBlockIdx(); tileIdx < (kernelTiling).totalTileNum; tileIdx += blockNum) { \
+    uint64_t blockNum = 1;       \
+	  for(uint64_t tileIdx = 0; tileIdx < (kernelTiling).totalTileNum; tileIdx += blockNum) { \
         (kernelTiling).CaclOneTileParam(tileIdx); \
         SWIGLU_SINGLE_PROCESS_TILE_NON32BALIGNED((kernelTiling).offsetParam, *((kernelTiling).curTileCopyParam), (kernelTiling).curCalLen); \
 	} \
diff --git a/activation/swi_glu/op_kernel/swi_glu.cpp b/activation/swi_glu/op_kernel/swi_glu.cpp
index 0450bfbf..2e57d990 100644
--- a/activation/swi_glu/op_kernel/swi_glu.cpp
+++ b/activation/swi_glu/op_kernel/swi_glu.cpp
@@ -14,2 +14,4 @@
  */
+#ifndef OPP_MEGA_MOE_SWI_GLU_CPP
+#define OPP_MEGA_MOE_SWI_GLU_CPP
 #include "swi_glu_impl.hpp"
@@ -19,3 +21,3 @@
 using namespace AscendC;
-extern "C" __global__ __aicore__ void swi_glu(GM_ADDR input_gm, GM_ADDR output_gm,
+extern "C" inline __aicore__ void swi_glu(GM_ADDR input_gm, GM_ADDR output_gm,
                                               GM_ADDR workspace, GM_ADDR tiling) {
@@ -25,18 +27,10 @@ extern "C" __global__ __aicore__ void swi_glu(GM_ADDR input_gm, GM_ADDR output_g
 
-  // DT_FLOAT = 0,            // float type
-  // DT_FLOAT16 = 1,          // fp16 type
-  // DT_BF16 = 27,            // bf16 type
-  GET_TILING_DATA(tempTilingGm, tiling);
-  if (tempTilingGm.isDoubleBuffer == 1) {
-    if (TILING_KEY_IS(1)) {
-      SwigluSingle<SwigluVectorBF16<half, float, half, 2>, half, half> op;
-      op.Init(input_gm, nullptr, output_gm, tiling);
-      op.Process();
-    } else if (TILING_KEY_IS(0)) {
-      SwigluSingle<SwigluVector<float, float, 2>, float, float> op;
+  GET_TILING_DATA_WITH_STRUCT(SwiGluTilingData, tempTilingGm, tiling);
+
+  if constexpr (std::is_same_v<DTYPE_Y, bfloat16_t>) {
+    if (tempTilingGm.isDoubleBuffer == 1) {
+      SwigluSingle<SwigluVectorBF16<bfloat16_t, float, bfloat16_t, 2>, bfloat16_t, bfloat16_t> op;
       op.Init(input_gm, nullptr, output_gm, tiling);
       op.Process();
-    }
-#if defined(__CCE_AICORE__) && __CCE_AICORE__  == 220
-    else if (TILING_KEY_IS(27)) {
+    } else {
       SwigluSingle<SwigluVectorBF16<bfloat16_t, float, bfloat16_t, 2>, bfloat16_t, bfloat16_t> op;
@@ -45,16 +39,9 @@ extern "C" __global__ __aicore__ void swi_glu(GM_ADDR input_gm, GM_ADDR output_g
     }
-#endif
   } else {
-    if (TILING_KEY_IS(1)) {
-      SwigluSingle<SwigluVectorBF16<half, float, half, 1>, half, half> op;
-      op.Init(input_gm, nullptr, output_gm, tiling);
-      op.Process();
-    } else if (TILING_KEY_IS(0)) {
-      SwigluSingle<SwigluVector<float, float, 1>, float, float> op;
+    if (tempTilingGm.isDoubleBuffer == 1) {
+      SwigluSingle<SwigluVectorBF16<half, float, half, 2>, half, half> op;
       op.Init(input_gm, nullptr, output_gm, tiling);
       op.Process();
-    }
-#if defined(__CCE_AICORE__) && __CCE_AICORE__  == 220
-    else if (TILING_KEY_IS(27)) {
-      SwigluSingle<SwigluVectorBF16<bfloat16_t, float, bfloat16_t, 1>, bfloat16_t, bfloat16_t> op;
+    } else {
+      SwigluSingle<SwigluVectorBF16<half, float, half, 1>, half, half> op;
       op.Init(input_gm, nullptr, output_gm, tiling);
@@ -62,4 +49,4 @@ extern "C" __global__ __aicore__ void swi_glu(GM_ADDR input_gm, GM_ADDR output_g
     }
-#endif
   }
-}
\ No newline at end of file
+}
+#endif  // OPP_MEGA_MOE_SWI_GLU_CPP
diff --git a/activation/swi_glu_grad/op_kernel/swi_glu_grad.cpp b/activation/swi_glu_grad/op_kernel/swi_glu_grad.cpp
index 2e386402..f672fbb1 100644
--- a/activation/swi_glu_grad/op_kernel/swi_glu_grad.cpp
+++ b/activation/swi_glu_grad/op_kernel/swi_glu_grad.cpp
@@ -17,45 +17,31 @@
 #include "swi_glu_grad_single.hpp"
 
 using namespace AscendC;
-extern "C" __global__ __aicore__ void swi_glu_grad(GM_ADDR gradout_gm, GM_ADDR input_gm, GM_ADDR output_gm,
+extern "C" inline __aicore__ void swi_glu_grad(GM_ADDR gradout_gm, GM_ADDR input_gm, GM_ADDR output_gm,
                                                    GM_ADDR workspace, GM_ADDR tiling) {
     // DT_FLOAT = 0,            // float type
     // DT_FLOAT16 = 1,          // fp16 type
     // DT_BF16 = 27,            // bf16 type
-    GET_TILING_DATA(tempTilingGm, tiling);
-    if (tempTilingGm.isDoubleBuffer == 1) {
-      if (TILING_KEY_IS(1)) {
-        SwiGluGradSingle <SwiGluGradBF16<half, float, half, 2>, half, half> op;
-        op.Init(gradout_gm, input_gm, output_gm, tiling);
-        op.Process();
-      } else if (TILING_KEY_IS(0)) {
-        SwiGluGradSingle<SwiGluGradVector<float, float, float, float, float, 2>, float, float> op;
+    GET_TILING_DATA_WITH_STRUCT(SwiGluTilingData, tempTilingGm, tiling);
+    if constexpr (std::is_same_v<DTYPE_DY, bfloat16_t>) {
+      if (tempTilingGm.isDoubleBuffer == 1) {
+        SwiGluGradSingle <SwiGluGradBF16<bfloat16_t, float, bfloat16_t, 2>, bfloat16_t, bfloat16_t> op;
         op.Init(gradout_gm, input_gm, output_gm, tiling);
         op.Process();
-      }
-#if defined(__CCE_AICORE__) && __CCE_AICORE__  == 220
-      else if (TILING_KEY_IS(27)) {
-        SwiGluGradSingle <SwiGluGradBF16<bfloat16_t, float, bfloat16_t, 2>, bfloat16_t, bfloat16_t> op;
+      } else {
+        SwiGluGradSingle <SwiGluGradBF16<bfloat16_t, float, bfloat16_t, 1>, bfloat16_t, bfloat16_t> op;
         op.Init(gradout_gm, input_gm, output_gm, tiling);
         op.Process();
       }
-#endif
     } else {
-      if (TILING_KEY_IS(1)) {
-        SwiGluGradSingle <SwiGluGradBF16<half, float, half, 1>, half, half> op;
-        op.Init(gradout_gm, input_gm, output_gm, tiling);
-        op.Process();
-      } else if (TILING_KEY_IS(0)) {
-        SwiGluGradSingle<SwiGluGradVector<float, float, float, float, float, 1>, float, float> op;
+      if (tempTilingGm.isDoubleBuffer == 1) {
+        SwiGluGradSingle <SwiGluGradBF16<half, float, half, 2>, half, half> op;
         op.Init(gradout_gm, input_gm, output_gm, tiling);
         op.Process();
-      }
-#if defined(__CCE_AICORE__) && __CCE_AICORE__  == 220
-      else if (TILING_KEY_IS(27)) {
-        SwiGluGradSingle <SwiGluGradBF16<bfloat16_t, float, bfloat16_t, 1>, bfloat16_t, bfloat16_t> op;
+      } else {
+        SwiGluGradSingle <SwiGluGradBF16<half, float, half, 1>, half, half> op;
         op.Init(gradout_gm, input_gm, output_gm, tiling);
         op.Process();
       }
-#endif
   }
 }
diff --git a/cmake/variables.cmake b/cmake/variables.cmake
index 86398c47..7b1d914a 100644
--- a/cmake/variables.cmake
+++ b/cmake/variables.cmake
@@ -12,2 +12,2 @@
-set(OP_CATEGORY_LIST "matmul" "conv" "activation" "foreach" "hash" "vfusion" "index" "loss" "norm" "optim" "pooling" "quant" "rnn" "control")
+set(OP_CATEGORY_LIST "mega_moe")
 
diff --git a/cmake/third_party/opbase.cmake b/cmake/third_party/opbase.cmake
--- a/cmake/third_party/opbase.cmake
+++ b/cmake/third_party/opbase.cmake
@@ -17,16 +17,6 @@ elseif(EXISTS "${CANN_3RD_LIB_PATH}/opbase")
   get_filename_component(OPBASE_SOURCE_PATH
                          ${CANN_3RD_LIB_PATH}/opbase REALPATH)
   message(STATUS "Find opbase source dir: ${OPBASE_SOURCE_PATH}")
-  execute_process(
-    COMMAND git checkout ${OPBASE_TAG_ID}
-    WORKING_DIRECTORY ${OPBASE_SOURCE_PATH}
-    RESULT_VARIABLE EXEC_RESULT
-    OUTPUT_VARIABLE EXEC_INFO
-    ERROR_VARIABLE EXEC_ERROR
-  )
-  if(${EXEC_RESULT})
-    message(FATAL_ERROR "Git checkout failed! error: ${EXEC_ERROR}")
-  endif()
 else()
   if(EXISTS "${PROJECT_SOURCE_DIR}/build/_deps/opbase-subbuild")
     file(REMOVE_RECURSE ${PROJECT_SOURCE_DIR}/build/_deps/opbase-subbuild)
diff --git a/cmake/gen_ops_info.cmake b/cmake/gen_ops_info.cmake
index 181a0db2..39b4e413 100644
--- a/cmake/gen_ops_info.cmake
+++ b/cmake/gen_ops_info.cmake
@@ -261,7 +261,9 @@ function(merge_graph_headers)
   set(oneValueArgs TARGET OUT_DIR)
   cmake_parse_arguments(MGPROTO "" "${oneValueArgs}" "" ${ARGN})
   get_target_property(proto_headers ${GRAPH_PLUGIN_NAME}_proto_headers INTERFACE_SOURCES)
-  set(proto_headers ${proto_headers} ${CMAKE_SOURCE_DIR}/common/inc/op_graph/op_nn_proto_extend.h)
+  if(NOT ENABLE_CUSTOM)
+    list(APPEND proto_headers ${CMAKE_SOURCE_DIR}/common/inc/op_graph/op_nn_proto_extend.h)
+  endif()
   add_custom_command(OUTPUT ${MGPROTO_OUT_DIR}/ops_proto_nn.h
     COMMAND ${ASCEND_PYTHON_EXECUTABLE} ${CMAKE_SOURCE_DIR}/scripts/util/merge_proto.py
     ${proto_headers}
diff --git a/scripts/kernel/binary_script/build_env.sh b/scripts/kernel/binary_script/build_env.sh
index 72a4514a..cc5bb319 100644
--- a/scripts/kernel/binary_script/build_env.sh
+++ b/scripts/kernel/binary_script/build_env.sh
@@ -24,7 +24,7 @@ SOC_MAP=([Ascend310P]="Ascend310P3"
 
 OPC_TASK_NAME="opc_cmd.sh"
 OUT_TASK_NAME="out_cmd.sh"
-OP_CATEGORY_LIST="matmul conv activation foreach vfusion index loss norm optim pooling quant rnn control"
+OP_CATEGORY_LIST="mega_moe"
 
 function trans_soc() {
   local _soc_input=$1
diff --git a/scripts/util/ascendc_impl_build.py b/scripts/util/ascendc_impl_build.py
index b106f3ca..12c5f54d 100644
--- a/scripts/util/ascendc_impl_build.py
+++ b/scripts/util/ascendc_impl_build.py
@@ -166,6 +166,11 @@ def {}({}, kernel_name="{}", impl_mode=""):
     __inputs__, __outputs__, __attrs__ = _build_args({})
     options = get_dtype_fmt_options(__inputs__, __outputs__)
     options += ["-x", "cce"]
+    ascend_shmem_path = os.environ.get('ASCEND_SHMEM_HOME_PATH')
+    if ascend_shmem_path != None:
+        ascend_shmem_path = os.path.realpath(ascend_shmem_path)
+    else:
+        print("please export ASCEND_SHMEM_HOME_PATH")
     bisheng = os.environ.get('BISHENG_REAL_PATH')
     if bisheng is None:
         bisheng = shutil.which("bisheng")
@@ -185,6 +190,15 @@ def {}({}, kernel_name="{}", impl_mode=""):
         "..", "..", "..", "latest", os.uname().machine+"-linux", "asc", "atcos"))
     options.append("-I" + os.path.join(PYF_PATH, "..", "ascendc", "common"))
 
+    options.append("-I" + os.path.join(ascend_shmem_path, "shmem"))
+    options.append("-I" + os.path.join(ascend_shmem_path, "shmem", "src"))
+    options.append("-I" + os.path.join(ascend_shmem_path, "shmem", "src", "device"))
+    options.append("-I" + os.path.join(ascend_shmem_path, "shmem", "src", "host_device"))
+    options.append("-I" + os.path.join(ascend_shmem_path, "shmem", "include"))
+    options.append("-I" + os.path.join(ascend_shmem_path, "shmem", "include", "device"))
+    options.append("-I" + os.path.join(ascend_shmem_path, "shmem", "include", "host_device"))
+    options.append("-I" + os.path.join(ascend_shmem_path, "shmem", "include", "host"))
+
     ascend_home_path = os.environ.get('ASCEND_HOME_PATH')
     if ascend_home_path is None:
         ascend_home_path = os.path.realpath("/usr/local/Ascend/cann")
@@ -227,9 +241,17 @@ REPLAY_OP_API = '''
     else:
         op_replay_path = os.path.join(PYF_PATH, "..", "op_replay")
     replayapi_so_path = os.path.join(op_replay_path, "libreplay_{}_" + soc_short + ".so")
+    shmem_so_path = os.path.join(ascend_shmem_path, "shmem", "lib", "libhyper_parallel_shmem.so")
+    libshmem_utils = os.path.join(ascend_shmem_path, "shmem", "lib", "libhyper_parallel_shmem_utils.so")
+    libshmem_bootstrap_uid = os.path.join(ascend_shmem_path, "shmem", "lib", "aclshmem_bootstrap_uid.so")
+    libshmem_bootstrap_config_store = os.path.join(ascend_shmem_path, "shmem", "lib", "aclshmem_bootstrap_config_store.so")
     load_dso(codegen_so_path)
     load_dso(replaystub_so_path)
     load_dso(replayapi_so_path)
+    load_dso(shmem_so_path)
+    load_dso(libshmem_utils)
+    load_dso(libshmem_bootstrap_uid)
+    load_dso(libshmem_bootstrap_config_store)
     op_type = "{}"
     entry_obj = os.path.join(op_replay_path, "{}_entry_" + soc_short + ".o")
     code_channel = get_code_channel(src, kernel_name, op_type, options)
diff --git a/install_deps.sh b/install_deps.sh
index 6c47bbe6..c055160a 100644
--- a/install_deps.sh
+++ b/install_deps.sh
@@ -423,29 +423,11 @@ install_patch() {
 }

 check_dependencies_silent() {
-    local args=("$@")
-    local check_pkgz="false"
-    local check_dos2unix="false"
-
-    for arg in "${args[@]}"; do
-        case "$arg" in
-            --pkg)
-                check_pkgz="true"
-                check_dos2unix="true"
-                ;;
-            --opkernel)
-                check_dos2unix="true"
-                ;;
-        esac
-    done
-
     local missing_deps=()
     declare -A req_versions
     req_versions["Python"]="3.7.0"
     req_versions["GCC"]="7.3.0"
     req_versions["CMake"]="3.16.0"
-    req_versions["pigz"]="2.4"
-    req_versions["dos2unix"]=""

     check_deps() {
         local name="$1"
@@ -483,13 +465,6 @@ check_dependencies_silent() {
     check_deps "Python" "python3" "${req_versions["Python"]}"
     check_deps "GCC" "gcc" "${req_versions["GCC"]}"
     check_deps "CMake" "cmake" "${req_versions["CMake"]}"
-    if [[ "$check_dos2unix" == "true" ]]; then
-        check_deps "dos2unix" "dos2unix" "${req_versions["dos2unix"]}"
-    fi
-    if [[ "$check_pkgz" == "true" ]]; then
-        check_deps "pigz" "pigz" "${req_versions["pigz"]}"
-    fi
-
     if [[ ${#missing_deps[@]} -eq 0 ]]; then
         return 0
     else
